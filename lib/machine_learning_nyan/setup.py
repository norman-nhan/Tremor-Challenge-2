from setuptools import setup, find_packages

setup(
    name='preprocessing_nyan',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'pandas',
        'matplotlib',
    ],
    author='TANG PHU THIEN NHAN',
    description='A library for machine learning tasks',
)